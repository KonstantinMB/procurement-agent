// Legacy scripted demo (deprecated). The command bar always drives the real
// Claude Agent SDK via /api/command + startAgentRun(). This file is no longer
// imported anywhere; kept only as historical reference.
export function runDemo(): void {
  // intentionally empty — multi-RFQ mode replaces the scripted demo path.
}
